package com.lld.designpatterns.behavioural.strategy;

// step 2 - write proper implementations for each strategy.
public class FourWheerlerStrategy implements NavigationStrategy {
    @Override
    public Double navigate(String from, String to) {
        return null;
    }
}
